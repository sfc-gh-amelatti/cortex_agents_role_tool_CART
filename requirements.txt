# Requirements for Automated Agent Permissions Notebook
# This file contains all the Python packages needed to run the automated agent permissions notebook

# Core Python libraries (usually included with Python)
# json - built-in
# os - built-in  
# tempfile - built-in
# datetime - built-in
# typing - built-in (Python 3.5+)

# External packages that need to be installed
requests>=2.25.0
PyYAML>=5.4.0
python-dotenv>=0.19.0

# Snowflake packages
snowflake-connector-python>=2.7.0
snowflake-snowpark-python>=1.0.0

# Optional: Jupyter notebook dependencies (if running in Jupyter)
jupyter>=1.0.0
ipykernel>=6.0.0
notebook>=6.4.0

# Optional: Development and testing dependencies
pytest>=6.0.0
black>=21.0.0
flake8>=3.9.0
